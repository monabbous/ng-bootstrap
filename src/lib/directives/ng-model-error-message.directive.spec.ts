import { NgModelErrorMessageDirective } from './ng-model-error-message.directive';

describe('NgModelErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
