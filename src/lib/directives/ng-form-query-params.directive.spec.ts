import { NgFormQueryParamsDirective } from './ng-form-query-params.directive';

describe('NgFormQueryParamsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFormQueryParamsDirective();
    expect(directive).toBeTruthy();
  });
});
